# Настройка GitHub для проекта

## 📑 Содержание

### Часть 1: Первоначальная настройка
1. [Настройка Git конфигурации](#1-настройка-git-конфигурации)
2. [Создание репозитория на GitHub](#2-создание-репозитория-на-github)
3. [Подключение локального репозитория к GitHub](#3-подключение-локального-репозитория-к-github)
4. [Настройка SSH ключей](#4-настройка-ssh-ключей-рекомендуется-для-долгосрочной-работы)
5. [Клонирование через Cursor](#5-клонирование-репозитория-через-cursor)

### Часть 2: Работа с Git через командную строку
- [Команды для работы с Git](#команды-для-работы-с-git)
- [Решение проблем с аутентификацией](#решение-проблем-с-аутентификацией)
- [Результат успешной настройки](#-результат-успешной-настройки)

### Часть 3: Работа с GitHub в Cursor
- [Интерфейс Source Control](#интерфейс-source-control)
- [Основные операции](#основные-операции-в-cursor)
- [Полезные расширения](#полезные-расширения-для-git-в-cursor)
- [Настройки Git в Cursor](#настройки-git-в-cursor)
- [Типичные сценарии работы](#типичные-сценарии-работы)
- [Горячие клавиши](#горячие-клавиши-для-git-в-cursor)
- [Troubleshooting](#troubleshooting-в-cursor)

---

## Шаги для подключения к GitHub

### 1. Настройка Git конфигурации
```powershell
git config --global user.name "Pavanrog"
git config --global user.email "rpa3611@gmail.com"
```

### 2. Создание репозитория на GitHub
1. Перейдите на https://github.com
2. Нажмите кнопку "New repository"
3. Введите название репозитория: `1C_Development_Project`
4. Выберите "Private" или "Public"
5. НЕ добавляйте README, .gitignore или лицензию (они уже созданы)
6. Нажмите "Create repository"

### 3. Подключение локального репозитория к GitHub

#### Вариант A: Использование Personal Access Token (рекомендуется)
1. Создайте Personal Access Token на GitHub:
   - Перейдите в Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Нажмите "Generate new token (classic)"
   - Выберите scopes: `repo`, `workflow`, `write:packages`
   - Скопируйте токен (он показывается только один раз!)

2. Выполните команды:
```powershell
# Очистите кэш учетных данных
git config --global --unset credential.helper

# Настройте URL с токеном (замените ВАШ_ТОКЕН на реальный токен)
git remote set-url origin https://Pavanrog:ВАШ_ТОКЕН@github.com/Pavanrog/1C_Development_Project.git

# Добавьте файлы в индекс
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Project structure and documentation"

# Отправьте в GitHub
git push -u origin main
```

**Альтернативный способ** - при запросе логина:
- Username: `Pavanrog`
- Password: `ваш_токен` (НЕ пароль от GitHub!)

#### Вариант B: Использование SSH (альтернативный)
Если настроили SSH ключи, используйте SSH URL:
```powershell
git remote set-url origin git@github.com:Pavanrog/1C_Development_Project.git
git push -u origin main
```

### 4. Настройка SSH ключей (рекомендуется для долгосрочной работы)

```powershell
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "rpa3611@gmail.com"
# Нажмите Enter для сохранения в стандартное расположение (~/.ssh/id_ed25519)
# Можете задать passphrase или оставить пустым

# Запуск ssh-agent
Start-Service ssh-agent

# Добавление ключа в ssh-agent
ssh-add $env:USERPROFILE\.ssh\id_ed25519

# Копирование публичного ключа в буфер обмена
Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub | Set-Clipboard

# Или просто просмотр:
type $env:USERPROFILE\.ssh\id_ed25519.pub
```

#### Добавление ключа в GitHub:
1. Перейдите на GitHub: Settings → SSH and GPG keys
2. Нажмите "New SSH key"
3. Задайте имя (например, "Cursor на рабочем компьютере")
4. Вставьте скопированный публичный ключ
5. Нажмите "Add SSH key"

#### Тестирование SSH соединения:
```powershell
ssh -T git@github.com
# Должно вывести: "Hi Pavanrog! You've successfully authenticated..."
```

### 5. Клонирование репозитория через Cursor
1. Откройте Cursor
2. Нажмите `Ctrl+Shift+P`
3. Выберите "Git: Clone"
4. Введите URL репозитория: `https://github.com/Pavanrog/1C_Development_Project.git`
5. Выберите папку для клонирования
6. Cursor автоматически откроет склонированный проект

## Команды для работы с Git

### Основные команды
```powershell
# Проверка статуса
git status

# Добавление файлов
git add .
git add filename

# Коммит
git commit -m "Описание изменений"

# Отправка в GitHub
git push

# Получение изменений
git pull

# Просмотр истории
git log --oneline
```

### Работа с ветками
```powershell
# Создание новой ветки
git checkout -b feature/new-feature

# Переключение между ветками
git checkout main
git checkout feature/new-feature

# Слияние веток
git merge feature/new-feature
```

## Решение проблем с аутентификацией

### ✅ Успешное решение (проверено)
**Проблема**: `remote: Invalid username or token. Password authentication is not supported for Git operations.`

**Решение**:
1. Создайте Personal Access Token на GitHub
2. Очистите кэш Git: `git config --global --unset credential.helper`
3. Настройте URL с токеном: `git remote set-url origin https://Pavanrog:ВАШ_ТОКЕН@github.com/Pavanrog/1C_Development_Project.git`
4. Отправьте код: `git push -u origin main`

### Другие возможные ошибки

1. **Проверьте URL репозитория:**
```powershell
git remote -v
```

2. **Если нужно изменить URL на SSH:**
```powershell
git remote set-url origin git@github.com:Pavanrog/1C_Development_Project.git
```

3. **Если используете HTTPS с токеном:**
   - Убедитесь, что используете Personal Access Token, а не пароль
   - Токен должен иметь права `repo`

4. **Очистка кэша учетных данных (Windows):**
```powershell
git config --global --unset credential.helper
```

### Альтернативные способы аутентификации

#### Через GitHub CLI
```powershell
# Установите GitHub CLI с https://cli.github.com
# Затем выполните:
gh auth login
```

#### Через Cursor
1. Откройте Cursor
2. Ctrl+Shift+P → "Git: Clone"
3. Введите URL репозитория
4. Cursor автоматически предложит аутентификацию

## ✅ Результат успешной настройки

После выполнения всех шагов у вас будет:
- **Локальный репозиторий** с полной структурой проекта 1С
- **Удаленный репозиторий** на GitHub: https://github.com/Pavanrog/1C_Development_Project
- **Настроенная аутентификация** через Personal Access Token
- **Все файлы загружены** в репозиторий (179 объектов, 122.03 KiB)

### Проверка успешной настройки:
```powershell
git status
# Должно показать: "Your branch is up to date with 'origin/main'"

git log --oneline -5
# Должно показать историю коммитов
```

## Рекомендации

1. **Регулярно делайте коммиты** с описательными сообщениями
2. **Используйте ветки** для новых функций
3. **Следуйте соглашениям** именования коммитов
4. **Не коммитьте** временные файлы и конфиденциальную информацию
5. **Сохраните токен** в безопасном месте (действует 90 дней)

## Структура коммитов

Рекомендуемый формат сообщений коммитов:
- `feat: добавить новую функцию`
- `fix: исправить ошибку в модуле`
- `docs: обновить документацию`
- `refactor: рефакторинг кода`
- `test: добавить тесты`

---

# Работа с GitHub в Cursor

## Интерфейс Source Control

### Открытие панели Source Control
Есть несколько способов открыть панель управления Git:
1. **Через иконку** в левой панели (третья сверху, иконка ветвления)
2. **Горячая клавиша**: `Ctrl+Shift+G`
3. **Командная палитра**: `Ctrl+Shift+P` → "View: Show Source Control"

### Элементы интерфейса Source Control

#### 1. **Индикатор изменений**
- В нижнем левом углу Cursor отображается текущая ветка и количество изменений
- Нажав на имя ветки, можно быстро переключиться на другую ветку

#### 2. **Список изменённых файлов**
В панели Source Control отображаются:
- **U** (Untracked) - новые неотслеживаемые файлы
- **M** (Modified) - изменённые файлы
- **D** (Deleted) - удалённые файлы
- **A** (Added) - добавленные в индекс файлы

#### 3. **Кнопки действий**
- **Commit** (✓) - создать коммит
- **Refresh** (↻) - обновить статус
- **More Actions** (...) - дополнительные действия

## Основные операции в Cursor

### 1. Просмотр изменений
**Способ A: Через список файлов**
- Кликните на файл в списке изменений
- Откроется diff-представление с подсветкой изменений
- Слева - старая версия, справа - новая версия

**Способ B: Inline diff**
- Изменения отображаются прямо в редакторе
- Зелёные линии - добавленные строки
- Красные линии - удалённые строки

### 2. Создание коммита (визуальный интерфейс)

#### Шаг 1: Добавление файлов в индекс (Stage)
**Вариант A: Добавить все файлы**
1. Откройте панель Source Control (`Ctrl+Shift+G`)
2. Наведите на "Changes" и нажмите `+` (Stage All Changes)

**Вариант B: Добавить отдельные файлы**
1. Наведите на нужный файл
2. Нажмите `+` рядом с файлом

**Вариант C: Добавить части файла**
1. Откройте файл с изменениями
2. В diff-представлении нажмите на `Stage Selected Ranges`
3. Можно добавить только определённые строки

#### Шаг 2: Создание коммита
1. Введите сообщение коммита в поле "Message"
2. Нажмите `Ctrl+Enter` или кнопку ✓ "Commit"

**Расширенные опции коммита:**
- **Commit & Push** - сделать коммит и сразу отправить в GitHub
- **Commit & Sync** - сделать коммит, получить изменения и отправить
- **Commit Staged** - закоммитить только файлы в индексе

### 3. Отправка изменений в GitHub (Push)

**Способ A: Через меню**
1. Откройте Source Control (`Ctrl+Shift+G`)
2. Нажмите `...` → `Push`

**Способ B: Через индикатор**
1. Нажмите на индикатор ветки в нижнем левом углу
2. Появится стрелка ↑ с числом (количество коммитов для отправки)
3. Нажмите на стрелку

**Способ C: Автоматическая синхронизация**
- После коммита нажмите `Sync Changes` в панели Source Control
- Это выполнит `git pull` и `git push` автоматически

### 4. Получение изменений из GitHub (Pull)

**Способ A: Через меню**
1. `Ctrl+Shift+G` → `...` → `Pull`

**Способ B: Через индикатор**
1. Если есть изменения на GitHub, в нижнем углу появится стрелка ↓
2. Нажмите на стрелку для получения изменений

**Способ C: Sync**
- Нажмите `Sync Changes` - выполнит pull и push одновременно

### 5. Работа с ветками

#### Создание новой ветки
**Способ A: Через командную палитру**
1. `Ctrl+Shift+P` → "Git: Create Branch"
2. Введите имя новой ветки (например, `feature/new-document`)
3. Нажмите Enter

**Способ B: Через индикатор ветки**
1. Нажмите на имя текущей ветки (нижний левой угол)
2. Выберите "Create new branch..."
3. Введите имя ветки

#### Переключение между ветками
1. Нажмите на имя текущей ветки
2. Выберите нужную ветку из списка
3. Cursor переключит ветку и обновит файлы

#### Слияние веток (Merge)
1. Переключитесь на целевую ветку (например, `main`)
2. `Ctrl+Shift+P` → "Git: Merge Branch"
3. Выберите ветку для слияния
4. Если есть конфликты, Cursor предложит их разрешить

### 6. Просмотр истории коммитов

**Через командную палитру:**
1. `Ctrl+Shift+P` → "Git: View History"
2. Откроется список коммитов с авторами и датами

**Через расширение GitLens (рекомендуется):**
1. Установите расширение GitLens
2. Откройте файл
3. В каждой строке будет отображаться информация о последнем изменении
4. Нажав на информацию, можно увидеть полный коммит

### 7. Отмена изменений

#### Отменить изменения в файле (до коммита)
1. В панели Source Control найдите файл
2. Нажмите на значок ↶ "Discard Changes"
3. Подтвердите действие

#### Отменить изменения в строках
1. Откройте файл с изменениями
2. В diff-представлении нажмите "Revert Selected Ranges"
3. Выбранные строки вернутся к исходному состоянию

#### Отменить коммит
1. `Ctrl+Shift+P` → "Git: Undo Last Commit"
2. Изменения вернутся в рабочую директорию

### 8. Разрешение конфликтов

Когда возникают конфликты при слиянии или pull:

1. **Cursor автоматически отметит конфликты** в файлах
2. Откройте файл с конфликтом
3. Cursor покажет блоки:
   ```
   <<<<<<< HEAD
   Ваши изменения
   =======
   Изменения из другой ветки
   >>>>>>> branch-name
   ```
4. Используйте кнопки:
   - **Accept Current Change** - принять ваши изменения
   - **Accept Incoming Change** - принять изменения из другой ветки
   - **Accept Both Changes** - оставить оба варианта
   - **Compare Changes** - сравнить варианты
5. После разрешения всех конфликтов:
   - Добавьте файлы в индекс
   - Создайте коммит слияния

### 9. Работа с .gitignore

#### Создание/редактирование .gitignore в Cursor
1. Откройте файл `.gitignore` (или создайте его в корне проекта)
2. Добавьте шаблоны файлов для игнорирования:
   ```gitignore
   # 1С временные файлы
   *.tmp
   *.bak
   *.log
   
   # Кэш и настройки IDE
   .vscode/
   .idea/
   
   # Конфиденциальные данные
   config/secrets.txt
   *.key
   ```

#### Быстрое добавление файла в .gitignore
1. В панели Source Control кликните правой кнопкой на файл
2. Выберите "Add to .gitignore"
3. Файл автоматически добавится в .gitignore

### 10. Просмотр изменений на GitHub

**Через Cursor:**
1. `Ctrl+Shift+P` → "Git: Open on GitHub"
2. Откроется браузер с репозиторием

**Для конкретного файла:**
1. Откройте файл
2. `Ctrl+Shift+P` → "Git: Open File on GitHub"

## Полезные расширения для Git в Cursor

### 1. **GitLens** (настоятельно рекомендуется)
**Установка:**
1. `Ctrl+Shift+X` → Поиск "GitLens"
2. Нажмите "Install"

**Возможности:**
- Blame annotations (кто и когда изменил каждую строку)
- История изменений файла
- Сравнение версий
- Визуализация веток
- Поиск по коммитам

### 2. **Git Graph**
**Возможности:**
- Визуальное представление истории веток
- Графическое отображение merge и branch
- Удобная навигация по коммитам

### 3. **Git History**
**Возможности:**
- Просмотр истории файлов
- Сравнение версий
- Поиск по истории

## Настройки Git в Cursor

### Открытие настроек
1. `Ctrl+,` → поиск "git"
2. Или `File` → `Preferences` → `Settings` → `Extensions` → `Git`

### Полезные настройки

#### Автоматическое получение изменений
```json
"git.autofetch": true
```
- Cursor автоматически проверяет изменения на GitHub каждые несколько минут

#### Подтверждение перед push
```json
"git.confirmSync": false
```
- Отключает подтверждение перед синхронизацией (удобно для частых коммитов)

#### Автоматическое добавление в индекс
```json
"git.enableSmartCommit": true
```
- При коммите автоматически добавляет все изменённые файлы

#### Показывать inline diff
```json
"git.decorations.enabled": true
```
- Показывает изменения прямо в редакторе

#### Формат сообщения коммита
```json
"git.inputValidation": "always"
```
- Проверяет формат сообщения коммита

## Типичные сценарии работы

### Сценарий 1: Ежедневная разработка
1. Утром: `Pull` для получения изменений от команды
2. Работаете над кодом
3. Периодически: `Stage` → `Commit` → `Push`
4. Вечером: финальный `Push`

### Сценарий 2: Работа над новой функцией
1. Создайте ветку: `feature/название-функции`
2. Разрабатывайте в этой ветке
3. Делайте коммиты с описательными сообщениями
4. Когда готово: переключитесь на `main` и сделайте `Merge`
5. Отправьте изменения: `Push`

### Сценарий 3: Исправление ошибки
1. Создайте ветку: `fix/описание-ошибки`
2. Исправьте ошибку
3. Коммит: `fix: описание исправления`
4. Merge в `main`
5. Push

### Сценарий 4: Синхронизация с удалённым репозиторием
1. Сделайте `Commit` локальных изменений
2. Выполните `Pull` для получения изменений с GitHub
3. Если есть конфликты - разрешите их
4. Выполните `Push` для отправки своих изменений

## Горячие клавиши для Git в Cursor

| Действие | Горячая клавиша |
|----------|----------------|
| Открыть Source Control | `Ctrl+Shift+G` |
| Коммит | `Ctrl+Enter` (в поле сообщения) |
| Командная палитра | `Ctrl+Shift+P` |
| Поиск команд Git | `Ctrl+Shift+P` → "Git:" |
| Открыть терминал | `Ctrl+`` (обратная кавычка) |
| Переключить ветку | Клик на имя ветки внизу |

## Интеграция с GitHub через Cursor

### Аутентификация в GitHub
1. При первом `Push` Cursor предложит авторизоваться
2. Выберите "Sign in with GitHub"
3. Откроется браузер для авторизации
4. После авторизации Cursor получит токен автоматически

### Работа с Pull Requests (требуется расширение)
1. Установите "GitHub Pull Requests and Issues"
2. `Ctrl+Shift+P` → "GitHub Pull Requests: Create Pull Request"
3. Заполните описание
4. Создайте PR прямо из Cursor

### Просмотр Issues
1. С расширением "GitHub Pull Requests and Issues"
2. Можно просматривать и создавать Issues
3. Привязывать коммиты к Issues

## Советы по работе с Git в Cursor

1. **Используйте визуальный интерфейс** для начала - это проще и нагляднее
2. **Изучите GitLens** - это значительно повысит продуктивность
3. **Делайте частые коммиты** с понятными сообщениями
4. **Проверяйте diff** перед коммитом
5. **Используйте ветки** для экспериментов
6. **Настройте автофetch** для актуальности данных
7. **Сохраните токен** если используете HTTPS
8. **Используйте SSH** для более безопасной работы

## Troubleshooting в Cursor

### Cursor не видит изменения
1. Обновите панель Source Control (кнопка ↻)
2. Перезапустите Cursor
3. Проверьте `.gitignore`

### Не удаётся сделать Push
1. Проверьте интернет-соединение
2. Проверьте аутентификацию: `git remote -v` в терминале
3. Сделайте `Pull` сначала - возможно есть изменения на GitHub

### Конфликты при Pull
1. Cursor автоматически покажет файлы с конфликтами
2. Разрешите конфликты через интерфейс
3. Добавьте файлы в индекс
4. Создайте merge commit

### Случайно закоммитили не то
1. `Ctrl+Shift+P` → "Git: Undo Last Commit"
2. Или в терминале: `git reset --soft HEAD~1`
3. Внесите правки и сделайте новый коммит
